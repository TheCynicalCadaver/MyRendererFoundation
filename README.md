I gave thematic names, will change most eventually ; this repository thus far is so heavily driven by 3rd party libraries that the main lang is c.

To compile this shit make a directory and run cmake.. and then make in it(the directory), if you don't have the dependencies; you will have to wait till I update the cmake file to gather them for you.
